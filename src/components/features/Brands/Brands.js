import styles from './Brands.module.scss';
import React from 'react';

const Brands = props => (
  <div className={styles.root}>
    <div className='container'>lolo</div>
  </div>
);

export default Brands;
